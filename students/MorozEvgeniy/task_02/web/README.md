# 🍝 Recipes App — Web (Frontend)

> **Вариант 18** — Клиентская часть full‑stack приложения  
> «Рецепты: *Что приготовить?*»

SPA‑приложение для поиска рецептов, планирования питания и ведения списка покупок.  
Frontend взаимодействует с REST API сервера, поддерживает аутентификацию пользователей и защищённые маршруты.

---

## 🛠 Стек технологий

- **React 19**
- **TypeScript**
- **Vite**
- **React Router DOM**
- **Axios**
- **CSS (custom, dark theme)**
- **Playwright** — e2e‑тесты
- **Docker + Nginx** — production‑сборка

---

## 📦 Установка и запуск

### 1️⃣ Установка зависимостей

```bash
pnpm install
```

---

### 2️⃣ Переменные окружения

Создай файл `.env` на основе `.env.example`:

```env
VITE_API_URL=http://localhost:3000
```

---

### 3️⃣ Запуск в режиме разработки

```bash
pnpm dev
```

Приложение будет доступно по адресу:  
👉 **<http://localhost:5173>**

---

### 4️⃣ Production‑сборка

```bash
pnpm build
pnpm preview
```

---

## 🧭 Страницы приложения

| Маршрут | Описание |
|-------|----------|
| `/login` | Вход пользователя |
| `/register` | Регистрация |
| `/recipes` | Список рецептов |
| `/recipes/:id` | Детали рецепта |
| `/mealplan` | План питания |
| `/shopping` | Список покупок |

---

## 🔐 Аутентификация

- Аутентификация реализована через **JWT**
- Access Token хранится в **localStorage**
- Запросы к API выполняются через **Axios**
- Защищённые маршруты требуют авторизации

---

## 🔗 Взаимодействие с API

Все HTTP‑запросы выполняются к backend‑серверу:

```text
VITE_API_URL → http://localhost:3000
```

Примеры ресурсов:

- `/auth`
- `/recipes`
- `/mealplan`
- `/shopping`

---

## 🎨 UI и стили

- Используется **кастомная CSS‑архитектура**
- Тёмная тема по умолчанию
- Компоненты:
  - карточки
  - формы
  - списки
- Адаптивные сетки (recipes grid)
- Состояния:
  - loading
  - error
  - empty
  - success

---

## 🧪 Тестирование

Для e2e‑тестирования используется **Playwright**:

```bash
pnpm playwright test
```

Покрываются сценарии:

- открытие страниц
- авторизация
- навигация

---

## 🐳 Docker (Production)

### Сборка образа

```bash
docker build -t recipes-web .
```

### Запуск контейнера

```bash
docker run -p 80:80 recipes-web
```

Приложение будет доступно по адресу:  
👉 **<http://localhost>**

---

## ✅ Реализовано (MVP)

- React SPA
- Маршрутизация
- JWT‑аутентификация
- Работа с REST API
- План питания
- Список покупок
- Тёмная тема UI
- Production‑сборка через Nginx
- E2E‑тесты
