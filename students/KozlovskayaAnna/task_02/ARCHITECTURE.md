# Архитектура системы управления мероприятиями

## Диаграмма компонентов

```mermaid
graph TB
    subgraph Browser["🌐 Браузер пользователя"]
        Client["<b>Клиентское приложение</b><br/>Next.js + React<br/>TypeScript"]
    end
    
    subgraph Server["🖥️ API Сервер"]
        Backend["<b>Серверное приложение</b><br/>Node.js + Express<br/>TypeScript"]
    end
    
    subgraph Database["💾 Хранение данных"]
        MongoDB["<b>База данных</b><br/>MongoDB<br/>(пользователи, события,<br/>билеты, приглашения)"]
    end
    
    subgraph Storage["☁️ Облачное хранилище"]
        S3["<b>Amazon S3</b><br/>(изображения мероприятий,<br/>фото спикеров)"]
    end
    
    Client -->|"HTTP запросы<br/>(JWT токены в headers)"| Backend
    Backend -->|"Ответы JSON<br/>(данные + токены)"| Client
    Backend -->|"CRUD операции<br/>(Mongoose)"| MongoDB
    MongoDB -->|"Результаты запросов"| Backend
    Backend -->|"Загрузка изображений<br/>(AWS SDK)"| S3
    S3 -->|"URL изображений"| Backend
    Client -.->|"Прямой доступ<br/>по публичным URL"| S3

    style Client fill:#61DAFB,stroke:#333,stroke-width:2px,color:#000
    style Backend fill:#68A063,stroke:#333,stroke-width:2px,color:#fff
    style MongoDB fill:#47A248,stroke:#333,stroke-width:2px,color:#fff
    style S3 fill:#FF9900,stroke:#333,stroke-width:2px,color:#000
    style Browser fill:#f0f0f0,stroke:#666,stroke-width:1px
    style Server fill:#f0f0f0,stroke:#666,stroke-width:1px
    style Database fill:#f0f0f0,stroke:#666,stroke-width:1px
    style Storage fill:#f0f0f0,stroke:#666,stroke-width:1px
```

## Описание взаимодействия компонентов

### 1. Клиентское приложение → API Сервер
- **Технология**: HTTP/HTTPS запросы
- **Аутентификация**: JWT токены в заголовках Authorization
- **Формат данных**: JSON

### 2. API Сервер → База данных
- **Технология**: MongoDB драйвер (Mongoose ODM)
- **Операции**: CRUD (Create, Read, Update, Delete)
- **Данные**: Пользователи, события, билеты, приглашения, спикеры

### 3. API Сервер → Amazon S3
- **Технология**: AWS SDK
- **Операции**: Загрузка и получение изображений
- **Данные**: Изображения мероприятий, фотографии спикеров

### 4. Клиентское приложение → Amazon S3
- **Технология**: Прямые HTTP запросы по публичным URL
- **Операции**: Получение изображений для отображения
